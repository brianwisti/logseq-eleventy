{% extends "layouts/base.njk" %}

{% from "macros/graph.njk" import render_page_note %}

{% block main %}
  <article class="page">
    {% if content %}
      {{ content | safe }}
    {% endif %}

    {{ render_page_note(page) }}
  </article>
{% endblock %}
